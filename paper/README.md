# Paper draft (ICLR 2026 template)

`main.tex` + `refs.bib` + the template files (`iclr2026_conference.{sty,bst}`, `natbib.sty`, `fancyhdr.sty`, `math_commands.tex`)
and `figures/`. No LaTeX toolchain is installed on the laptop: compile on Overleaf (upload this folder) or with
`latexmk -pdf main.tex`. Red `[TODO: ...]` marks are the places the GPU results fill in (agent tables, leaderboards,
decision environments, power calculation, author block). `\iclrfinalcopy` is on for reading; remove it for anonymous submission.
